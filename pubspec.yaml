name: kicc_community_app
description: A prototype mobile app for KICC community with short courses.
publish_to: none
environment:
  sdk: "^3.1.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true